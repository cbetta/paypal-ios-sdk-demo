# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/ios'
require 'bubble-wrap'
require 'motion-cocoapods'

Motion::Project::App.setup do |app|
  app.name = 'paypal-ios-sdk-demo'
  app.pods do
    pod 'PayPal-iOS-SDK', '~> 1.0.4'
  end
end
